# Optional: an LLM key if you want to swap the heuristic core for an LLM pass.
# The pipeline runs fully offline WITHOUT any key (heuristic core + live HTTP verification).
# OPENAI_API_KEY=sk-...
# COMPOSIO_API_KEY=...   # optional, for the optional MCP adapter (see src/mcp_adapter.py)
